name: Nightly Health
on:
  schedule:
    - cron: "15 6 * * *"
  workflow_dispatch:
jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pulse
        run: |
          echo "files=$(find . -type f | wc -l)"
          git log -1 --oneline
